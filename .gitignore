# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.env

# IDE / OS
.vscode/
.idea/
.DS_Store
*.swp
*~

# Jupyter
.ipynb_checkpoints/

# Large binary data (place .npy files here locally; not tracked)
data/**/raw/*.npy
data/**/resampled/*.npy

# Regenerable cache (auto-created on first run)
data/zeta/*.npy

# Result PDFs under result/ are tracked for portfolio display.
# Re-run main.py to regenerate locally; commit updates when figures change.

# Optional: lock file from pip freeze (keep requirements.txt instead)
requirements-lock.txt
